name: CI

on: [push, pull_request, workflow_dispatch]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Reuse composite action
        uses: ./.github/actions/lint-and-test
